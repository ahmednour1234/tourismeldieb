@props(['value' => '4.8'])
<span class="inline-flex items-center gap-1 text-sm font-medium text-amber-700" aria-label="{{ $value }} rating">
    <span aria-hidden="true">*****</span>
    <span>{{ $value }}</span>
</span>
